Reverse digits
(it will remove zero)




class Solution {
    public int reverseDigits(int n) {
        // Code here
        int rev=0;
        while(n!=0){
            int last=n%10;
            n=n/10;
            rev=(rev*10)+last;
        }
    return rev;
    }
}
